[build]
builder = "dockerfile"
dockerfilePath = "services/api/Dockerfile"

[build.watchPaths]
paths = ["services/api/**", "packages/shared/**"]

[deploy]
startCommand = "/workspace/services/api/entrypoint.sh"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"